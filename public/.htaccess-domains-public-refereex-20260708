<IfModule mod_lsapi.c>
    php_value opcache.enable 0
    php_value opcache.enable_cli 0
    php_value auto_prepend_file "/home/u234903558/domains/sportifyplus.de/public_html/site-prepend-20260705-refereex-static.php"
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^only-talents/home/?$ /only-talents [R=301,L,QSA]
    RewriteRule ^public/only-talents/home/?$ /only-talents [R=301,L,QSA]

    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # RefereeX AI — static HTML (OPcache-safe)
    RewriteRule ^refereex-ai/?$ refereex-ai.html [L]

    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Fresh front controller filename bypasses frozen OPcache
    RewriteRule ^ index-20260708-refereex-live.php [L]
</IfModule>

Options -Indexes
